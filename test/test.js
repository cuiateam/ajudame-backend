// Arquivo JS para realização de todos os teste referente a API rest (aqui também ficara os exemplos de utilização)

new Vue({
  el: '#testTipoOng',
  data: {
    tiposOng: []
  }
});

new Vue({
  el: '#testTipoArquivo',
  data: {
    tiposArquivo: []
  }
});